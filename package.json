{
  "name": "unsubscribet",
  "version": "1.0.0",
  "description": "Demo platform to manage customer preference on their subscriptions",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
    "install-client": "cd client && npm install && npm run build && cd ..",
    "install-server": "cd server && npm install && cd .. ",
    "heroku-postbuild": "npm run install-server"
  },
  "keywords": [
    "example",
    "heroku"
  ],
  "devDependencies": {
      "concurrently": "^9.0.1"
    }
}
